# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# environments
.venv/
venv/
env/

# tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
coverage.xml

# harvest output — results are reproducible, so they do not belong in git
out/
*.jsonl
*.xlsx
*.csv
!tests/fixtures/*.csv

# browser drivers must never be committed again (the repo once held 35 MB of them)
chromedriver
chromedriver.exe
geckodriver
*.zip

# editor / OS
.DS_Store
.idea/
.vscode/
